# Client-only Vite variables. Do not put server secrets here.

# Browser requests stay same-origin so Vite can proxy /login, /me, etc.
VITE_API_BASE_URL=

# Vite dev server forwards API requests to the Express backend.
VITE_API_PROXY_TARGET=http://localhost:4321
